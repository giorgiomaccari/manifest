AOSP MarshMallow for condor
===========

To initialize your local repository using the AOSP-RRO trees, use a command like this:
````bash
repo init -u git://github.com/giorgiomaccari/manifest.git -b marshmallow
```
Then to sync up:
````bash
repo sync
```
